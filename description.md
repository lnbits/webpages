# WebPages

Admin-only editor for managing static HTML, CSS, and JavaScript files served from `static/pages`.

Use it to create simple pages you can later expose via a reverse proxy (for example, Caddy).
